<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.mapper.SafetyCheckMapper">

    <!-- 1. 마스터 데이터 저장 (시퀀스 미리 채번하여 DTO의 checkId에 세팅) -->
    <insert id="insertMaster" parameterType="com.spring.dto.SafetyCheckMasterDTO">
        <selectKey keyProperty="checkId" resultType="long" order="BEFORE">
            SELECT SEQ_SAFETY_CHECK_MASTER.NEXTVAL FROM DUAL
        </selectKey>
        INSERT INTO SAFETY_CHECK_MASTER (
            CHECK_ID,
            CHECK_DATE,
            CHECK_ROUND,
            INSPECTOR,
            REG_DATE
        ) VALUES (
            #{checkId},
            #{checkDate},
            #{checkRound},
            #{inspector},
            SYSDATE
        )
    </insert>

    <!-- 2. 상세 항목 데이터 저장 -->
    <insert id="insertDetail" parameterType="com.spring.dto.SafetyCheckDetailDTO">
        INSERT INTO SAFETY_CHECK_DETAIL (
            DETAIL_ID,
            CHECK_ID,
            ITEM_NO,
            STATUS_CODE,
            REMARK
        ) VALUES (
            SEQ_SAFETY_CHECK_DETAIL.NEXTVAL,
            #{checkId},
            #{itemNo},
            #{statusCode},
            #{remark, jdbcType=VARCHAR}
        )
    </insert>
    
    <!-- 가장 최근 작성된 마스터 1건 조회 -->
    <select id="selectLatestMaster" resultType="com.spring.dto.SafetyCheckMasterDTO">
        SELECT *
        FROM (
            SELECT 
                CHECK_ID AS checkId,
                CHECK_DATE AS checkDate,
                CHECK_ROUND AS checkRound,
                INSPECTOR AS inspector
            FROM SAFETY_CHECK_MASTER
            ORDER BY CHECK_ID DESC
        )
        WHERE ROWNUM = 1
    </select>

    <!-- 특정 CHECK_ID에 해당하는 상세 20개 항목 조회 -->
    <select id="selectDetailsByCheckId" parameterType="long" resultType="com.spring.dto.SafetyCheckDetailDTO">
        SELECT 
            DETAIL_ID AS detailId,
            CHECK_ID AS checkId,
            ITEM_NO AS itemNo,
            STATUS_CODE AS statusCode,
            REMARK AS remark
        FROM SAFETY_CHECK_DETAIL
        WHERE CHECK_ID = #{checkId}
        ORDER BY DETAIL_ID ASC
    </select>

</mapper>
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.mapper.AreaMapper">

    <!-- 신규 INSERT -->
    <insert id="insertAreaConfig">
        INSERT INTO FESTIVAL_AREA_CONFIG (CONFIG_ID, MAP_NAME, CONFIG_JSON, UPDATED_AT)
        VALUES (SEQ_AREA_CONFIG.NEXTVAL, #{mapName}, #{configJson, jdbcType=CLOB}, SYSDATE)
    </insert>

    <!-- UPDATE (id 이름을 updateAreaConfig로 변경) -->
    <update id="updateAreaConfig">
        UPDATE FESTIVAL_AREA_CONFIG
           SET CONFIG_JSON = #{configJson, jdbcType=CLOB},
               UPDATED_AT = SYSDATE
         WHERE MAP_NAME = #{mapName}
    </update>

    <!-- 도면 배치 JSON 조회 -->
    <select id="selectAreaConfig" resultType="string">
        SELECT CONFIG_JSON
          FROM FESTIVAL_AREA_CONFIG
         WHERE MAP_NAME = #{mapName}
    </select>

</mapper>